# 每日论文简报 2026-09-20

> 图卡版见邮件附件 2026-09-20.html

> 主题方向：傅里叶光学 / 精密测量 / 工业检测机器学习
> 数据来源：arXiv（export.arxiv.org），共 0 篇匹配论文。
> 说明：arXiv API 元数据不含作者单位，故仅列作者姓名，不作推测；脚本每篇最多保留 6 位作者，超过者未列出。
> 抓取记录：**原生脚本 19 次请求再次全部 HTTP 406（连续第三期复现），产出「今日无匹配新论文」的假零**；换 curl 传输层后**零重试一次跑通**，结果同样是 0 篇候选，但**跳过历史重复 24 篇**——这个健康指标不为零，说明数据确实取到了，只是全被去重滤掉。为把「真空窗」与「又一次静默漏检」区分开，另从 arXiv 列表页对四个分类的 09-18 公告日做了**穷尽复核：174 条条目、剔重后 167 篇，命中 `server_terms` 的是 0 篇**。**结论：今天是真空窗，不是故障。** 详见文末「运行记录」。

---

## 一、今日论文

**无。** 今天没有可报道的论文，且这个「无」是**经独立穷尽复核确认的真空窗**，与 9 月 14 日那次「故障造成的假零」性质完全不同，需要分开读：

1. **脚本的读数这次是可信的。** 关键区别在于**跳过历史重复 24 篇**。9 月 14 日那次抓取全败时，这个数字是 **0**——说明一条 entry 都没拿到；今天它是 **24**，落在正常区间（近期为 18～24 篇），说明 API 把窗口内的论文完整交了出来，只是这 24 篇**昨天及更早已经全部报道过**，去重后自然剩零。**这正是 9 月 14 日简报里提出的「把跳过历史重复数当健康指标看」那条建议，今天第一次用它做出了正确的阳性判断。**

2. **实质层面的「无」是复核出来的。** 绕开 API，从 arXiv 列表页取回 physics.optics / eess.IV / cs.CV / eess.SP 四类在最新公告日 **Fri, 18 Sep 2026** 的全部条目（四类页面本次**均为完整显示、无截断**），合并去重 174 条，剔除 `digests/` 与 `digests_am/` 中 443 个历史已报道 ID 后剩 **167 篇**，逐篇取回摘要原文，用 `fetch_arxiv.py` 自己的 `relevance_score()` 与 `server_terms` 短语表复算：**167 篇命中 `server_terms` 的是 0 篇。** 即便索引再往前走一步，脚本在这批里也一篇都不会召回。

3. **为什么今天本来就不该有新东西——是周末，不是索引冻结。** 这一点今天差点被误判，值得写清楚：索引前沿 `2026-09-17T17:59:58Z` **与昨日运行时是同一秒**，滞后从 27.2h 涨到 **51.2h**，乍看像是 9 月 13 日那种「索引冻结」重演。但列表页显示四个分类的最新公告日都已推进到 **Fri, 18 Sep 2026**，而那一批公告对应的正是**截止到 `09-17T17:59:58Z` 的投稿**——**前沿与公告批次严丝合缝，索引没有冻结**。真正的原因是 arXiv 周五、周六晚间不公告：`51.2 − 27.2 = 24.0h`，**多出来的正好是一整天**，是停更而非故障。昨日那一轮已经把 Fri 18 Sep 这批吃干净了，今天到下一批（周一 9/21 公告）之前没有任何新增。

所以今天没有论文可读，这是真的，且成因干净：**上一批已消化完毕 + 周末无新公告**。

---

## 运行记录

### 一、原生脚本：HTTP 406 连续第三期复现，又一次假零

`python fetch_arxiv.py --profile main`（2026-09-19 21:03 UTC / 09-20 06:03 JST）：

| 阶段 | 结果 |
|---|---|
| 索引前沿探测 | 失败，`HTTP Error 406: Not Acceptable` → 退回按当前时刻计算窗口 |
| 第 0 页 | 6 次重试全败，全部 406 |
| 第 1 页 | 6 次重试全败，全部 406 |
| 第 2 页 | 6 次重试全败，全部 406 |
| 最终输出 | **0 篇候选，跳过历史重复 0 篇** |

脚本仍以退出码 0 正常结束，`candidates.md` 写入「今日无匹配新论文」。**这是 9/18、9/19 之后 406 的第三次复现**，且本期**没有出现 9/19 那样「十分钟后自行恢复」的情况**——从首跑到复核结束的约十分钟内，urllib 通路一次都没通过。

**注意：如果只看这一轮，今天会被记成「抓取失败、结果不可信」；如果只看候选篇数，又会被记成「空窗」。两种读法今天都是错的**——真相要靠下面两步才能定。

### 二、换 curl 传输层复跑：零重试跑通，证实 406 只卡在 urllib

按 9/19 简报给出的「最小改动」建议，写了一个 shim：**只把 `fetch_page()` 与 `probe_index_frontier()` 里的 `urllib.request.urlopen` 换成 `curl` 子进程，窗口计算、打分、跨天去重等全部逻辑原样复用**，随后重跑：

```
[main] 完成：0 篇候选（2026-09-20，窗口 72h，跳过历史重复 24），已写入 papers.json 与 candidates.md
[main] 索引前沿 2026-09-17T17:59:58Z（滞后 51.2h），窗口起点 2026-09-14T17:59:58Z
```

- **零重试、零失败**——19 次请求在 urllib 下全军覆没，换成 curl 后一次通过。加上 9/19 记录的「同进程 requests 同 URL 返回 200」，**406 的根因可以钉死在 urllib 客户端的传输层，与限流、网络放行、出口 IP 均无关。**
- 单独用 curl 直取 `cat:physics.optics&max_results=3` 也是 `HTTP 200`，再次交叉印证。
- **跳过历史重复 24 篇**（首跑是 0 篇）——这一个数字就把「假零」和「真空窗」分开了。

### 三、索引前沿：滞后 51.2h，但不是冻结

| 项目 | 本期 | 昨日（9/19） | 差值 |
|---|---|---|---|
| 索引前沿 | `2026-09-17T17:59:58Z` | `2026-09-17T17:59:58Z` | **同一秒** |
| 滞后 | **51.2 h** | 27.2 h | **+24.0 h** |
| 窗口起点 | `2026-09-14T17:59:58Z` | `2026-09-14T17:59:58Z` | 一致 |

前沿纹丝不动、滞后整整多出 24 小时，形态上与 9/13 的索引冻结高度相似，**但定性相反**。判据是列表页：

| 分类 | 最新公告日 | 该日条目数 | 是否截断 |
|---|---|---|---|
| physics.optics | Fri, 18 Sep 2026 | 22 | 否（22/22） |
| eess.IV | Fri, 18 Sep 2026 | 17 | 否（17/17） |
| cs.CV | Fri, 18 Sep 2026 | 125 | 否（125/125） |
| eess.SP | Fri, 18 Sep 2026 | 17 | 否（17/17） |

**Fri 18 Sep 这批公告覆盖的就是截止到 `09-17T17:59:58Z` 的投稿，与 API 前沿完全对齐**——索引没有落后于公告，只是**公告本身停了**：arXiv 周五、周六晚间不发布，下一批要等周一 9/21。**「前沿不动 + 滞后恰好 +24.0h + 公告日与前沿对齐」这三条同时成立，就是周末停更的指纹，不是冻结。** 记下这个判据，以免下次把周末误报成故障。

### 四、穷尽复核：167 篇，零命中

为排除「索引其实漏了一批」的可能，做了一次不依赖 `submittedDate` 索引的独立核对：

- **枚举**：从 `arxiv.org/list/<cat>/recent?show=2000` 取四个分类 Fri 18 Sep 公告日的全部条目（用 `show=2000` 规避 9/14 那次 50 条截断的问题，本次四类**全部完整显示**），合并去重 **174 条**。
- **去重**：剔除 `digests/` 与 `digests_am/` 中 **443** 个历史已报道 ID，剩 **167 篇**。
- **取摘要**：用 API 的 `id_list=` 批量取元数据——**这条通路不经过 `submittedDate` 索引**，因此即使索引有问题也能拿到真实数据，167 篇全部取回成功。
- **复算**：用脚本自己的 `relevance_score()` 与还原出的 24 条 `server_terms` 短语表逐篇打分。

结果：**167 篇里命中 `server_terms` 的是 0 篇。** 打分 >0 的有 18 篇，但全部是靠 `deep learning` / `detection` / `segmentation` 这类泛词蹭分，主题与本方向无关，得分最高的几篇是：

| arXiv ID | 分 | 题名（截断） | 方向 |
|---|---|---|---|
| 2609.20290 | 5 | TinyCNN: A 193K-Parameter Network for On-Device Plant Disease Detection | 农业端侧 CNN |
| 2609.19279 | 5 | Radio-Frequency Convolutional Neural Networks | 射频神经网络 |
| 2609.19178 | 4 | Sustainable MRI: Interpretable Deep Learning for estimating energy… | 医学影像能耗 |
| 2609.20523 | 3 | Noise-Robust Quantum State Characterization for Remote State Preparation | 量子态表征 |
| 2609.19592 | 3 | Selective Cotton Boll Localization for Robotic Harvesting | 农业机器人 |

**没有一篇同时满足 `server_terms`，因此没有一篇是被故障漏掉的。** 这轮复核的覆盖率**没有 9/14 那次的缺口**——四个分类本期都是完整显示，`cs.CV` 的 125 条也全部核到，**可以确认本方向四个分类在最新公告日无漏检**。

（一处小出入：`eess.IV` 页首标注 17 条、解析取回 18 条，`cs.CV` 标注 125 条、取回 126 条，多出的是段内交叉列出的条目。多取不会造成漏检，且这两条多出的 ID 也都参与了打分、同样零命中。）

### 五、处置建议

1. **406 的修复该落地了，不能再等。** 连续三期复现、本期**没有自愈**，而 curl 通路三期三次全部一次成功。9/19 已经提出「换传输层」，今天的证据把它从建议变成了必需：把 `fetch_page()` 与 `probe_index_frontier()` 的 `urllib.request` 换成 `requests`，或直接固化本期用的 curl 子进程方案。**在修好之前，每期都必须手动跑一次 curl 复核，否则随时可能把真论文当成空窗漏掉。**

2. **`candidates.md` 的失败语义仍然没修，今天再次显出危害。** 19 次请求全败，脚本照样退出码 0、照样写出格式完整的「今日无匹配新论文」。这条建议 9/14 提过一次，**至今未落地**。最小修法不变：**成功页数为 0 时以非零码退出**，并在 `candidates.md` 顶部写明「抓取失败，本文件不代表检索结果」。

3. **「跳过历史重复数」这个健康指标今天证明有效，建议把它写进脚本输出的显著位置。** 今天正是靠它（0 → 24）在十几秒内区分了假零与真空窗。可以直接在脚本里加一条判据：**候选为 0 且跳过重复也为 0 时，明确打印告警**，把这层人工判断固化下来。

4. **周末停更的指纹建议记进脚本注释**：前沿与昨日同一秒、滞后恰好多出 24.0h、列表页最新公告日与前沿对齐——三条同时成立即为周末停更，不必按故障处理。

5. **下一批是周一 9/21 公告，会有积压。** 周五、周六、周日三天的投稿将在周一一次性释放，命中数很可能顶到 `top_n: 12` 的上限。建议**周一临时把 `top_n` 调到 20** 跑一次，避免低分论文被截断、拖到次日才补上。

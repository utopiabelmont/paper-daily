# 每日论文简报 2026-09-14

> 图卡版见邮件附件 2026-09-14.html

> 主题方向：傅里叶光学 / 精密测量 / 工业检测机器学习
> 数据来源：arXiv（export.arxiv.org），共 0 篇匹配论文。
> 说明：arXiv API 元数据不含作者单位，故仅列作者姓名，不作推测；脚本每篇最多保留 6 位作者，超过者未列出。
> 抓取记录：**本期脚本一篇都没取到，原因是 `export.arxiv.org/api/query` 全程限流（HTTP 429 / 503），不是选题空窗，也不是索引冻结。** 两轮执行（06:03 JST 首跑超时、06:51 JST 冷却后重跑）里，索引前沿探测与全部 3 个数据页无一成功，单页 6 次重试全部被拒。**因此 `candidates.md` 里那句「今日无匹配新论文」是抓取失败造成的假阴性，不能按字面读。** 为把真实情况查清，另走 arxiv.org 列表页 / abs 页这条未被限流的通路做了独立复核，结论是：核心两个分类在本窗口内确实没有本方向的新论文（详见文末「运行记录」）。

---

## 一、今日论文

**无。** 本期没有可报道的论文，且这个「无」有两层成因，必须分开讲：

1. **脚本层面的「无」是故障造成的**，读数不可信——0 篇候选、**跳过历史重复也是 0 篇**。正常运行时这个跳过数是 20 篇上下（昨日 23 篇），它归零说明脚本根本没拿到任何一条 entry，而不是拿到了然后被过滤掉。
2. **实质层面的「无」是独立复核出来的，读数可信**——绕开被限流的 API，从 arxiv.org 列表页取回 physics.optics 与 eess.IV 在 9/10、9/11 两个公告日的全部条目，剔除历史已报道的之后剩 35 篇，逐篇抓 abs 页取摘要、用脚本自己的打分与服务端词条复算：**35 篇里命中 `server_terms` 的是 0 篇**。也就是说，即便 API 当时是通的，脚本在这两个分类里也一篇都不会召回。

所以今天没有论文可读，这一点是真的；但它是「周末 + 本方向确实没新东西」的结果，**不是**「索引又冻住了」——昨日那轮索引冻结今天已经确认解除。

---

## 运行记录

### 一、抓取执行情况：两轮，全部被 API 限流拦死

- **首跑**（2026-09-14 06:03 JST / 09-13 21:03 UTC）：`python fetch_arxiv.py --profile main` 跑满 600 秒未结束，被超时中止。事后看，时间全耗在 6 次重试 × 90 秒超时的等待上。
- **限流探测**（06:20–06:36 JST）：为判断是瞬时抖动还是持续限流，对 `export.arxiv.org/api/query` 做了 9 次独立探测，间隔 90 秒。**9 次无一成功**——6 次直接返回 `HTTP 429`、响应体是 arXiv 自己的 `Rate exceeded.`，3 次连响应都等不到（60 秒超时，curl exit 28）。
- **冷却重跑**（06:51:09 JST 起跑，07:03:02 JST 结束）：静默 12 分钟后重跑，仍然全线失败。逐项记录：

  | 阶段 | 结果 |
  |---|---|
  | 索引前沿探测 | 失败，`HTTP Error 429` → 脚本退回按当前时刻计算窗口 |
  | 第 0 页 | 6 次重试全败（429×4、503×2） |
  | 第 1 页 | 6 次重试全败（429×4、503×2） |
  | 第 2 页 | 6 次重试全败（429×6） |
  | 最终输出 | **0 篇候选，跳过历史重复 0 篇** |

- 脚本以退出码 0 正常结束，`candidates.md` 写入「今日无匹配新论文」。**这正是昨日简报预警过的失效模式的升级版**：昨日是前沿探测失败导致窗口算错、结果看似正常；今天是三个数据页全军覆没，脚本照样退 0、照样写出一份格式完整的「无匹配」候选清单，**表面上与真正的空窗日毫无区别**。

### 二、关键区分：是限流，不是网络封锁

这两件事后果完全不同（限流会自行恢复、封锁需要改环境），今天有明确证据可以定性——**arXiv 的其他端点全部畅通**：

| 端点 | 结果 |
|---|---|
| `export.arxiv.org/api/query` | **HTTP 429 / 503，持续 1 小时** |
| `arxiv.org/list/physics.optics/recent` | HTTP 200，97 KB |
| `arxiv.org/list/{cs.CV, eess.IV, eess.SP}/recent` | HTTP 200 |
| `rss.arxiv.org/rss/physics.optics` | HTTP 200 |
| `arxiv.org/abs/<id>`（连取 35 篇） | HTTP 200，**35/35 全部成功** |

同一条出口链路上，abs 页能连取 35 次不出错，API 端点连 1 次都过不去。**结论：`export.arxiv.org` 是放行的，今天是 arXiv 服务端对 API 端点的速率限制，与本环境的网络策略无关。** 另外首跑之前本次会话没有发起过任何 arXiv 请求，首跑第一发就是 429，说明限流不是被我们自己的重试打出来的，更可能是出口 IP 被其他流量共享所致。

### 三、昨日的索引冻结：已确认解除

昨日的核心问题是 submittedDate 索引逐秒冻结在 `2026-09-10T17:59:55Z` 整整 24 小时。今天 API 不通、无法直接探测前沿，改用列表页判定，证据一致指向解冻：

| 分类 | 最新公告日 | 该日条目数 | 最大 arXiv ID |
|---|---|---|---|
| physics.optics | Fri, 11 Sep 2026 | 14 | 2609.11858 |
| cs.CV | Fri, 11 Sep 2026 | 89（页面只列前 50） | 2609.11929 |
| eess.IV | Fri, 11 Sep 2026 | 9 | 2609.11506 |
| eess.SP | Fri, 11 Sep 2026 | 24 | 2609.11894 |

四个分类齐刷刷推进到 9/11 公告日，ID 段从昨日的 `2609.109xx` 前进到 `2609.119xx`。抽查两篇最新的 `2609.11858`、`2609.11809`，abs 页显示投稿日均为 **10 Sep 2026**，已越过昨日那个冻住的前沿 `09-10T17:59:55Z`。**昨日提出的「若明日前沿仍不动就把 window_hours 放宽到 120h 补召」这条预案，今天不必执行了。**

### 四、为什么今天本来也不会有论文：周末 + 本方向确实空窗

**其一，今天是 arXiv 的休息日。** 执行时刻 UTC 是周日（JST 已是周一清早）。`rss.arxiv.org/rss/physics.optics` 的 `skipDays` 明确列出 Saturday 与 Sunday，今天的 RSS 拉回来 `lastBuildDate` 是 `Sun, 13 Sep 2026 04:00:08 +0000`，**channel 里一个 item 都没有**——不是我们没抓到，是 arXiv 周末本来就不公告。下一批公告是周一 9/14，对应 9/11 及之后投稿的论文。

**其二，上一批公告里也没有本方向的东西，这是复核出来的，不是推测。** 具体做法：取 physics.optics 与 eess.IV 在 9/10、9/11 两个公告日的全部条目（这两类页面均为完整显示，无截断），对 `digests/` 与 `digests_am/` 里 349 个历史已报道 ID 去重后剩 **35 篇**，逐篇抓 abs 页取回摘要原文，再用 `fetch_arxiv.py` 自己的 `relevance_score()` 与还原出的 `server_terms` 短语表复算。结果：

- **`server_terms` 命中数：35 篇全部为 0。** 脚本的服务端查询要求摘要里至少出现 24 个 `abs:"…"` 短语之一（edge localization / defect detection / metrology / phase retrieval / wafer / wavefront 之类），**一篇都没命中**。这意味着即使 API 当时畅通，这 35 篇也不会有任何一篇进入候选。
- 打分 >0 的只有 5 篇，且全部是靠 `deep learning` / `detection` 这类泛词蹭到的分，与本方向无关：`2609.09577` 超声心肌应变漂移校正（4 分）、`2609.10825` 多模态 MRI 脑转移检测（3 分）、`2609.10065` 织物结构识别的视觉语言模型仲裁（1 分）、`2609.09825` 分形位错系统涡旋孤子阵列（1 分）、`2609.09189` 宫颈细胞学分类集成选择（1 分）。**其中没有一篇同时满足 `server_terms`，因此没有一篇是被故障漏掉的。**
- physics.optics 这 20 篇新条目按主题看确实离本方向很远：非厄米光学与拓扑光子（`2609.11577`、`2609.10011`、`2609.09312`、`2609.10061`）、高次谐波与强场（`2609.11858`、`2609.10235`）、纳米光子材料与量子发光（`2609.11501`、`2609.09771`、`2609.11809`）、光子晶体与仿真方法（`2609.09810`、`2609.10076`）。值得一提的是 `2609.10076`（RCWA 的 S 矩阵算法扩展）与 `2609.10086`（菲涅尔波带片实验室 X 射线显微）从题名看与衍射建模、散射测量沾边，但摘要均未触及测量、检测、计量任何一条词条，**脚本不召回它们是规则的正确执行，不是漏检**。

**覆盖率的诚实说明：这次复核只穷尽了 physics.optics 与 eess.IV 两类。** cs.CV 的 9/11 公告页只显示 89 条中的前 50 条，eess.SP 的 9/10 公告页只显示 30 条中的 26 条，这两类没有做穷尽核对。所以准确的表述是：**本方向最核心的两个分类可以确认无漏检；cs.CV / eess.SP 存在小比例未核条目，理论上仍可能藏有命中项。** 这部分缺口会在明日 API 恢复后由脚本的 72h 窗口自动覆盖——窗口起点会落在 9/11 之前，跨天去重按 arXiv ID 做，补召不会造成重复推送。

### 五、明日处置建议

1. **先探一次再跑全量。** 明早第一件事是单发一条最轻的 API 请求探活（`cat:physics.optics&max_results=1`）。返回 200 再跑 `fetch_arxiv.py`；若仍是 429，**不要立刻重试**——今天的教训是连续重试完全无效，9 次探测加 18 次脚本内重试无一成功，反而可能延长冷却。正确做法是间隔 30 分钟以上再探。
2. **明日是周一，会有积压涌入。** 周末两天的投稿加上 9/11 之后的新投稿会在周一公告日一次性释放，命中数很可能顶到 `top_n: 12` 的上限。建议明日**临时把 `top_n` 调到 20** 跑一次，把积压吃干净，避免重演 9/12→9/13 那种「低分论文被截断、次日才补上」的滞后。
3. **脚本有两个该补的硬约束，今天把优先级顶上来了**（昨日已提第一条，今天又踩中，且叠加了新的一条）：
   - **前沿探测失败时不应静默退回当前时刻**，应退回「昨日记录的前沿」或直接非零退出。
   - **全部数据页都抓取失败时不应退出码 0、更不应写出「今日无匹配新论文」。** 今天 3 个页全败仍然退 0，产出的候选清单与真正的空窗日在格式上完全一致，只有「跳过历史重复 0 篇」这一个不起眼的数字能看出异常。建议加一条判据：若成功页数为 0，则以非零码退出，并在 `candidates.md` 顶部写明「抓取失败，本文件不代表检索结果」。
4. **把「跳过历史重复数」当作健康指标看。** 这个数正常在 20 上下，一旦归零基本可以断定是抓取失败而非空窗，比看候选篇数更灵敏。
